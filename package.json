{
  "name": "next-apigw-todo-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/jest",
      "**/jest/**"
    ]
  },
  "scripts": {
    "infra-backend:test": "yarn workspace infra-backend test",
    "infra-backend:update-ss": "yarn workspace infra-backend test -- -u",
    "infra-backend:synth": "yarn workspace infra-backend synth",
    "infra-backend:diff": "yarn workspace infra-backend diff",
    "infra-backend:deploy": "yarn backend-api:deploy && yarn frontend-deployment:deploy",
    "backend-api:test": "yarn workspace infra-backend backend-api:test",
    "frontend-deployment:test": "yarn workspace infra-backend frontend-deployment:test",
    "backend-api:deploy": "yarn workspace infra-backend backend-api:deploy",
    "frontend-deployment:deploy": "yarn workspace infra-backend frontend-deployment:deploy",
    "frontend:dev": "yarn workspace frontend dev",
    "frontend:test": "yarn workspace frontend test",
    "frontend:build": "yarn workspace frontend build",
    "frontend:storybook": "yarn workspace frontend storybook"
  }
}
