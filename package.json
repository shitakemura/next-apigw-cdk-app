{
  "name": "next-apigw-todo-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/jest",
      "**/jest/**"
    ]
  },
  "scripts": {
    "backend:test": "yarn workspace infra-backend test",
    "backend:update-ss": "yarn workspace infra-backend test -- -u",
    "backend:synth": "yarn workspace infra-backend synth",
    "backend:diff": "yarn workspace infra-backend diff",
    "backend:deploy": "yarn workspace infra-backend deploy",
    "frontend:dev": "yarn workspace frontend dev"
  }
}
