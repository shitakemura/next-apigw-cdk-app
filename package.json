{
  "name": "next-apigw-todo-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/jest",
      "**/jest/**"
    ]
  },
  "scripts": {
    "infra-backend:test": "yarn workspace infra-backend test",
    "infra-backend:update-snapshot": "yarn workspace infra-backend test -- -u",
    "infra-backend:synth": "yarn workspace infra-backend synth",
    "infra-backend:deploy": "yarn workspace infra-backend deploy"
  }
}
